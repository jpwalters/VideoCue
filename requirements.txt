PyQt6>=6.5.0
pygame>=2.5.0
qdarkstyle>=3.1
numpy>=1.24.0
ruff>=0.1.0

# Alternative controller library for better compatibility
# Try: pip install inputs
# inputs>=0.5

# Optional: NDI video streaming support
# NDI bindings are now bundled with VideoCue (videocue/ndi_wrapper/)
# Based on ndi-python by Naoto Kondo - see videocue/ndi_wrapper/LICENSE.md for attribution
#
# Requires NDI Runtime installed from https://ndi.tv/tools/
# Application gracefully falls back to IP-only mode without NDI installed
